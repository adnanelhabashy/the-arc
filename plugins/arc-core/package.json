{
  "name": "bb-plugin-arc-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Arc product service host: agents, accounts, and usage for the three Arc coding agents.",
  "engines": {
    "bb": ">=0.43",
    "bbPluginSdk": ">=0.4.99"
  },
  "bb": {
    "name": "Arc Core",
    "description": "Arc product service host: agents, accounts, and usage for the three Arc coding agents.",
    "branding": {
      "icon": "Layers"
    },
    "server": "./src/server.ts",
    "host": "./src/voice-host.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "prepare:bundled": "bb-plugin-build prepare-bundled"
  },
  "dependencies": {
    "@bb/arc-domains": "workspace:*",
    "bb-arc-voice-host": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@bb/plugin-build": "workspace:*",
    "@bb/tsconfig": "workspace:*",
    "@get-bb/plugin-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "vitest": "^4.1.1"
  }
}
